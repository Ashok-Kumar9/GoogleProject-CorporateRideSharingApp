import 'package:flutter/material.dart';

class GiveRide extends StatelessWidget {
  const GiveRide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

      color: Colors.blue,
    );
  }
}
